var idade = 19;
var podeBeber = (idade >= 18) ? 'Pode beber' : 'Não pode beber';
console.log(podeBeber);
// condição ? true : false

var possuiFaculdade = true;
if(possuiFaculdade)
    console.log('Sim, possui');
else
    console.log('Não possui');
// if/else funciona sem {} se possuir apenas uma linha de comando